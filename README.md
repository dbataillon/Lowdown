# Lowdown
Lowdown is an album logging application that lets users record the albums they listen to and rate them. Lowdown also has a speech-to-text functionality for users that would prefer to speak instead of type.
